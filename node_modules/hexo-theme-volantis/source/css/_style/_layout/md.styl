.md
  >p
    padding-top: 1px  // 进一步减小段落的上边距

  h1,h2,h3,h4,h5,h6
    position: relative
    pointer-events: none
    margin-top: 10px
    margin-bottom: 0 
    padding-top: 0     // 减小顶部的内边距
    padding-bottom: 0  // 减小底部的内边距
    text-align: left 
    font-weight: 500
    display: flex
    align-items: center // 垂直居中
    justify-content: left // 水平居左
    height: 3em // 设置合适的高度
    >a
      color: inherit
      pointer-events: auto
      &:hover
        color: $color-hover
    &:before
      content: ''
      display: block
      margin-top: 0 - $navbar-height +  $gap / 3 // 进一步减小间距
      height: $navbar-height + $gap * 1.2 // 调整高度
      visibility: hidden
      pointer-events: none

  h1,h2
    &:before
      margin-top: $gap-h2 - $navbar-height - $gap / 3  // 进一步调整 H1, H2 的间距

  .article-meta+h1,.article-meta+h2
    margin-top: $gap-h2 - $navbar-height - $gap * 1.5 // 缩小间距

  h3,h4,h5,h6
    margin-bottom: $gap-p / 3 // 更小的下边距

  h3
    &:before
      margin-top: $gap-h3 - $navbar-height - $gap / 3 // 更小的上边距

  h4
    &:before
      margin-top: $gap-h4 - $navbar-height - $gap / 3

  h5
    font-weight: bold

  h2+h3:before
    margin-top: $gap-h2 - $navbar-height - $gap * 1.5 // 缩小 H2 + H3 之间的间距

  blockquote
    padding: 6px 6px 6px 20px 

  ul,ol
    font-size: $fontsize-list
    list-style: initial
    padding-top: 0
    padding-left: 4px // 进一步缩小内边距
    margin-left: $gap / 3 // 更小的左边距
    margin-top: $gap-p / 3  // 更小的上边距
    margin-bottom: $gap-p / 3 // 更小的下边距
    ul,ol
      margin-top: 0
      margin-bottom: 0
    li
      margin-top: 0px
      margin-bottom: 0px
      li
        margin-top: 0
        margin-bottom: 0
        padding-top: 0
      p
        margin-top: 1px // 更小的段落间距
        margin-bottom: 0

  &.task-list
    padding-left: 0
    margin-left: 2px
    li
      list-style: none
      input
        margin-right: 2px

  ul>li
    list-style: initial
  ol>li
    list-style: decimal

  .div-ori-link
    display: block
    text-align: center
    margin: 1.5rem 0 // 缩小外边距

  .ori-link
    margin: auto
    padding: 0.6em 2em // 更小的内边距
    border: 1px solid $color-theme
    border-radius: $border-button
    color: $color-theme
    font-weight: 500
    &:hover
      color: $color-hover
      border-color: $color-hover
